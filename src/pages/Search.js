import React from 'react';
import BookSearch from '../components/BookSearch';

const Search = () => {
    return (
        <div>
            <BookSearch />
        </div>
    );
};

export default Search;
